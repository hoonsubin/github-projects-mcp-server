<decision_guidance>
  <principles>
    <principle>Do not include runtime implementation details (no function names, command names, UI entry points, or execution syntax)</principle>
    <principle>Prefer the smallest change that satisfies the request</principle>
    <principle>Prefer a single source of truth; avoid duplicated rules across files</principle>
    <principle>Ask a clarifying question only when critical ambiguity remains</principle>
  </principles>

  <boundaries>
    <rule>Define clear responsibilities and explicit handoff points to other modes</rule>
    <rule>When a request involves code implementation, architecture, or security — switch to the appropriate specialist mode</rule>
  </boundaries>

  <when_to_switch_modes>
    <scenario>Code implementation or refactoring</scenario>
    <switch_to>Code mode</switch_to>
  </when_to_switch_modes>

  <when_to_switch_modes>
    <scenario>System architecture or design decisions</scenario>
    <switch_to>Architect mode</switch_to>
  </when_to_switch_modes>

  <when_to_switch_modes>
    <scenario>Security audit or vulnerability review</scenario>
    <switch_to>Security Reviewer mode</switch_to>
  </when_to_switch_modes>

  <validation>
    <rule>After changes, scan for contradictions and update examples to match</rule>
    <rule>Ensure all vocabulary gaps reported by `scrum_orient` are surfaced to the user</rule>
  </validation>
</decision_guidance>
