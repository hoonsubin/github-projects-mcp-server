// =============================================================================
// src/domain/rules/labels.ts — DEPRECATED / TOMBSTONED
//
// classifyLabels and StoryTypeLabel are no longer used.
// Story type is now tracked via the Type project board field (type_display in
// config.yml + RuntimeConfig.typeOptions), not via repo labels.
//
// This file is kept as a tombstone to make the migration visible in git history.
// Safe to delete once the team is satisfied the old label-based type flow is gone.
// =============================================================================

// (no exports)
